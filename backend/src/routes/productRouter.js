import express from "express";
const productRouter = express.Router();

router.post("path", function1);

router.get("path", function2);

router.delete("path", function3);

router.patch("path", function4);

export default productRouter;
import express from "express";

const userRouter = express.Router();

router.post("/login", function1);

router.get("/logout", function2);

router.post("/register", function3);

export default userRouter;